<!-- Desktop Nav -->

<nav class="xs-col-3 lg-col-2 xs-p3 xs-fixed xs-t0 xs-b0 xs-l0 xs-overflow-auto fill-gray--lighter xs-border-right xs-hide sm-show">
  <h1 class="xs-mb2"><a href="/" class="solid-logo">Solid</a></h1>

  <ol class="list-unstyled solid-nav">
    <li>
      <h2 class="text-4 bold">Base</h2>
    </li>
    <% for(var slug in public.base._data){ %>
    <% var page = public.base %>
    <li>
    <a href="/base/<%= slug %>" class="xs-block <%- current.source == slug ? 'text-red' : '' %>"><%= page._data[slug].title %></a>
    </li>
    <% }; %>

    <li>
      <h2 class="text-4 bold xs-mt2">Interface</h2>
    </li>
    <% for(var slug in public.interface._data){ %>
    <% var page = public.interface %>
      <li>
        <a href="/interface/<%= slug %>" class="xs-block <%- current.source == slug ? 'text-red' : '' %>"><%= page._data[slug].title %></a>
      </li>
    <% }; %>

    <li>
      <h2 class="text-4 bold xs-mt2">Layout</h2>
    </li>
    <% for(var slug in public.layout._data){ %>
    <% var page = public.layout %>
      <li>
        <a href="/layout/<%= slug %>" class="xs-block <%- current.source == slug ? 'text-red' : '' %>"><%= page._data[slug].title %></a>
      </li>
    <% }; %>

    <li>
      <h2 class="text-4 bold xs-mt2">Resources</h2>
    </li>
    <% for(var slug in public.resources._data){ %>
    <% var page = public.resources %>
      <li>
        <a href="/resources/<%= slug %>" class="xs-block <%- current.source == slug ? 'text-red' : '' %>"><%= page._data[slug].title %></a>
      </li>
    <% }; %>

    <% for(var slug in public.sandbox._data){ %>
    <% var page = public.sandbox %>
      <li><a href="/sandbox/<%= slug %>" class="xs-block <%- current.source == slug ? 'text-red' : '' %>">Sandbox</a></li>
    <% }; %>
  </ol>
</nav>

<!-- Mobile Nav -->

<nav class="xs-col-12 fill-white xs-show sm-hide">
  <a href="#" class="text-2 vertical-center xs-block xs-p2 fill-gray--lighter xs-border-bottom--lighter text-gray bold nav-carat" onclick="toggle_visibility('solid-menu');"><span class="solid-logo-xs">Solid</span><span><%= title %></span></a>

  <ol class="list-unstyled" id="solid-menu" style="display:none;">
    <li>
      <a href="/" class="xs-block p2 border-bottom--lighter">Welcome</a>
    </li>
    <li>
      <h2 class="text-5 bold xs-px2 xs-py1 fill-gray--lighter xs-border-bottom--lighter">Base</h2>
    </li>
    <% for(var slug in public.base._data){ %>
    <% var page = public.base %>
      <li>
        <a href="/base/<%= slug %>" class="xs-block xs-p2 xs-border-bottom--lighter <%- current.source == slug ? 'text-red' : '' %>"><%= page._data[slug].title %></a>
      </li>
    <% }; %>

    <li>
      <h2 class="text-5 bold xs-px2 xs-py1 fill-gray--lighter xs-border-bottom--lighter">Interface</h2>
    </li>
    <% for(var slug in public.interface._data){ %>
    <% var page = public.interface %>
      <li>
        <a href="/interface/<%= slug %>" class="xs-block xs-p2 xs-border-bottom--lighter <%- current.source == slug ? 'text-red' : '' %>"><%= page._data[slug].title %></a>
      </li>
    <% }; %>

    <li>
      <h2 class="text-5 bold xs-px2 xs-py1 fill-gray--lighter xs-border-bottom--lighter">Layout</h2>
    </li>
    <% for(var slug in public.layout._data){ %>
    <% var page = public.layout %>
      <li>
        <a href="/layout/<%= slug %>" class="xs-block xs-p2 xs-border-bottom--lighter <%- current.source == slug ? 'text-red' : '' %>"><%= page._data[slug].title %></a>
      </li>
    <% }; %>

    <li>
      <h2 class="text-5 bold xs-px2 xs-py1 fill-gray--lighter xs-border-bottom--lighter">Resources</h2>
    </li>
    <% for(var slug in public.resources._data){ %>
    <% var page = public.resources %>
      <li>
        <a href="/resources/<%= slug %>" class="xs-block p2 border-bottom--lighter <%- current.source == slug ? 'text-red' : '' %>"><%= page._data[slug].title %></a>
      </li>
    <% }; %>

    <% for(var slug in public.sandbox._data){ %>
    <% var page = public.sandbox %>
      <li>
        <a href="/sandbox/<%= slug %>" class="xs-block xs-p2 text-orange xs-border-bottom--lighter <%- current.source == slug ? 'text-red' : '' %>">Sandbox</a>
      </li>
    <% }; %>
  </ol>
</nav>
